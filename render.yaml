services:
  - type: web
    name: nutcheck
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 24.14.1
      - key: DATA_DIR
        value: /tmp/nutcheck-data
